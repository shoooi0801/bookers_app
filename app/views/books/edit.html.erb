<div class="edit-contents">

  <h1 class="edit-subject">Editing Book</h1>

  <%= form_with model: @book, url: book_path(@book.id), method: :patch, local: true do |book| %>
    <% if @book.errors.any? %>
      <div id="error_explanation">
        <h2><%= pluralize(@book.errors.count, "error") %> prohibited this book from being saved:</h2>
        <ul>
          <% @book.errors.full_messages.each do |message| %>
            <li><%= message %></li>
          <% end %>
        </ul>
      </div>
  　<% end %>

    <div class="edit-content">
      <p class="edit-name">Title</p>
      <%= book.text_field :title %>
    </div>

    <div class="edit-content">
      <p class="edit-name">Boby</p>
      <%= book.text_area :body %>
    </div>

    <div class="update-btn"><%= book.submit 'Update Book' %></div>

  <% end %>

  <p><%= link_to "show", book_path(@book.id) %> | <%=link_to 'Back', books_path %></p>

</div>